{
  "name": "examples",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm i && vite",
    "build": "npm i && tsc && vite build",
    "preview": "npm i && vite preview"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.0.3"
  },
  "dependencies": {
    "gamepad-controller": "file:../gamepad-controller-1.0.0-rc.1.tgz"
  }
}
