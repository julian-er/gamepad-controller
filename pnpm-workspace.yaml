packages:
  - website

allowBuilds:
  '@parcel/watcher': true
  esbuild: true

# nwsapi >=2.2.26 recurses through element.matches for :modal under jsdom (~377ms per call). Remove when fixed upstream.
overrides:
  nwsapi: 2.2.25
