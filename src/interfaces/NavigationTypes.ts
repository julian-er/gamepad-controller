/** Cardinal directions used by all navigation functions. */
export type Direction = 'up' | 'down' | 'left' | 'right';

/** Shoulder buttons used for section/context navigation. */
export type ShoulderButton = 'R1' | 'L1';
